{
  "name": "foodfy-hono-bun-api",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot ./src/server.ts",
    "start": "bun run dist/server.js",
    "build": "bun build ./src/server.ts --outdir ./dist --format esm --target bun --minify --external hono"
  },
  "dependencies": {
    "hono": "4.7.7"
  },
  "devDependencies": {
    "@types/bun": "1.2.10"
  }
}