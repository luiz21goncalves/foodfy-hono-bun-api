{
  "name": "foodfy-hono-bun-api",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot ./src/server.ts",
    "start": "bun run dist/server.js",
    "build": "bun build ./src/server.ts --outdir ./dist --format esm --target bun --minify --packages external"
  },
  "dependencies": {
    "hono": "4.7.7",
    "hono-pino": "0.8.0",
    "pino": "9.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "1.2.10"
  }
}
